pipelines:
  Application:
    group: Development

    materials:
      AppGit:
        git: https://github.com/gocd-demo/2016-workshop.git
    stages:
      - build:
          jobs:
            build:
              tasks:
                - exec:
                    command: ./build_application.sh
              artifacts:
                - build:
                    source: output
      - unit_test:
          jobs:
            unit_test:
              tasks:
                - exec:
                    command: ./unit_test_application.sh
      - package:
          jobs:
            package:
              tasks:
                - fetch:
                    stage: build
                    job: build
                    source: output
                - exec:
                    command: ./package_application.sh
